# Issue #3 — rulebook markup vocabulary

Work-in-progress notes for `<Book>` / `<Section>` / `<PageBreak />` / `<ColumnBreak />` markup parsing and rendering. Superseded by the implementation in `src/core/`.
