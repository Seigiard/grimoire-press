## Agent skills

### Issue tracker

Issues live as GitHub issues in `Seigiard/grimoire-press`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The six triage state roles, using the default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
